# SLOB Example

A simple app showing the use of Design Pattern Serialized Large Object (SLOB)

ContainerFinder holds the reference to an instance of Container, and this to an instance of Box. All of them are a graph of objects. The whole graph is saved when serialising the ContainerFinder instance

## Run

Run TestSerialize
